CREATE TYPE util_meta.ut_parent_table AS (
        column_names text,
        parent_schema text,
        parent_name text,
        parent_full_name text,
        parent_noun text,
        parent_column_names text
    ) ;
